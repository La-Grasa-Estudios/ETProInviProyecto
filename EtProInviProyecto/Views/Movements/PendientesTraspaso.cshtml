@using EtPro.Models;
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@model IEnumerable<Movement>
@{
    ViewData["Title"] = "Traspasos Pendientes";
}

<h2>@ViewData["Title"]</h2>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">@TempData["Success"]</div>
}

@if (!Model.Any())
{
    <p>No hay solicitudes de traspaso pendientes.</p>
}
else
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Bien</th>
                <th>Origen</th>
                <th>Destino</th>
                <th>Solicitante</th>
                <th>Fecha solicitud</th>
                <th>Motivo</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Bien?.Nombre (@item.Bien?.NumeroIdentificacion)</td>
                    <td>@item.OriginDepartment?.Name</td>
                    <td>@item.DestinationDepartment?.Name</td>
                    <td>@item.UsuarioSolicitante?.UserName</td>
                    <td>@item.FechaSolicitud.ToString("dd/MM/yyyy HH:mm")</td>
                    <td>@item.Motivo</td>
                    <td>
                        <form asp-action="AprobarTraspaso" method="post" style="display:inline;">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="id" value="@item.Id" />
                            <button type="submit" class="btn btn-sm btn-success">Aprobar</button>
                        </form>
                        <form asp-action="RechazarTraspaso" method="post" style="display:inline;">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="id" value="@item.Id" />
                            <button type="submit" class="btn btn-sm btn-danger">Rechazar</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}